# Backup Netlify configuration
[build]
  command = "ROLLUP_SKIP_NATIVE=true npm run build"
  publish = "dist"

[build.environment]
  ROLLUP_SKIP_NATIVE = "true"
  NODE_OPTIONS = "--max_old_space_size=4096"

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 