# Disable all native modules to avoid platform-specific build issues
rollup-plugin-native=false
rollup_native=false
disable_binary=true
node_options=--max_old_space_size=4096
# rollup_skip_native=true
# Preferir versões pré-compiladas
# prefer-offline=true
# prefer-binary=true 